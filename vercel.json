{
  "buildCommand": "npx prisma generate --schema=prisma/schema.prisma && npx nx build frontend --configuration=production",
  "outputDirectory": "dist/apps/frontend/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
